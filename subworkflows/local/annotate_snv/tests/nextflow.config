params {
    snpeff_db                  = "WBcel235.115"
    outdir                     = "tests/results"
}

process {
    withName: 'ANNOTATE_SNV:SNPEFF_DOWNLOAD' {
        ext.prefix = { "${meta.id}" }
    }

    withName: 'ANNOTATE_SNV:SNPEFF_SNPEFF' {
        ext.prefix = { "${meta.id}_snpeff" }
        publishDir = [
            path: { "${params.outdir}/${meta.id}/snpeff" },
            mode: 'copy'
        ]
    }
}
